import { requestLogs } from './requestLog';
import { sessions } from './session';
import { users } from './user';

export const usersTable = users;
export const sessionsTable = sessions;
export const requestLogsTable = requestLogs;
